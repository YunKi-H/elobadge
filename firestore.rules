rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The web app talks to Fastify. Only the Admin SDK accesses Firestore.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
